name: qdm-feature-survival
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - tqdm
  - jupyter
  - ipykernel
  - pip:
      - torch
      - transformers
      - transformer-lens
      - sae-lens
      - datasets
      - huggingface-hub
      - accelerate
      - bitsandbytes
